1. Todo List
React Native + Expo
저장: @react-native-async-storage/async-storage
날짜선택: @react-native-community/datetimepicker
카메라/이미지: expo-image-picker
스타일: React Native StyleSheet
웹배포: Vercel + gitHub 연동

mobile install
Expo Go - testing program

create project
npx create-expo-app 폴더명 --template blank

run
npx expo start --clear --tunnel

picker install
npx expo install @react-native-community/datetimepicker

uuid
npm install react-native-uuid
import uuid from 'react-native-uuid';
const myUniqueId = uuid.v4();

date time picker install
npx expo install @react-native-community/datetimepicker

image picker install
npx expo install expo-image-picker

react-native-vector-icons
https://icons.expo.fyi/Index

react-native-vector-icons install
npm install --save react-native-vector-icons
import { Ionicons } from '@expo/vector-icons';

*
web deploy (Vercel) -> Netlify 비슷
npx expo install react-dom react-native-web
>
App.json Add "platforms"
{
  "expo": {
        "platforms": ["ios", "android", "web"],
        },
}

git push
https://vercel.com

Build Command
 npx expo export --platform web

 Output Directory
 dist
  > Deploy

vercel_URL = https://react-native-todos.vercel.app/

git push > continuous deploy(Netlify and Vercel)
*

*
deploy (Expo - Android SDK)
 npm install -g eas-cli
 eas login (email, pw) > Logged in
 eas init > Y
 eas build -p android --profile production (iphone ❌)
 ** Keystore? Y **
 이후 10~20분 소요

 Android app: (스토어에 올릴 때 필요한 파일, 테스팅 불가)
 https://expo.dev/artifacts/eas/ovSupS2WJSrEXJpDwpXo4s.aab

 eas.json에 추가 (SKD 생성하는 방법)
> 
  {
    "production": {
        "autoIncrement": true,
        "android": {
          "buildType": "apk"
        }
      }
  }

재빌드
eas build -p android --profile production
emulator? > no

apk파일 카톡전송 후 핸드폰 설치
� Android app:
https://expo.dev/artifacts/eas/2B9uNub8viysTqfurRWALJ.apk
*

install expo-linear-gradient
npx expo install expo-linear-gradient